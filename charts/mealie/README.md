# Mealie Helm Chart

This Helm chart deploys Mealie on Kubernetes.

## Installation

```bash
helm install mealie ./mealie
```

## Configuration

See `values.yaml` for configuration options.

## Parameters

| Name | Description | Value |
|------|-------------|-------|
| `replicaCount` | Number of replicas | `1` |
