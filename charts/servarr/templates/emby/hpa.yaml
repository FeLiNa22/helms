{{- if and .Values.emby.enabled .Values.emby.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-emby
  labels:
    {{- include "servarr.labels" (dict "context" . "appName" "emby") | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-emby
  minReplicas: {{ .Values.emby.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.emby.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.emby.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.emby.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.emby.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.emby.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
